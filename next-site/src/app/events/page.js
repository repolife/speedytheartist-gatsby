import { Collection } from '@/style/base'

export const metadata = {
  title: 'Events | Speedy The Artist',
}

export default function EventsPage() {
    return (
        <Collection>
            <iframe
                style={{ width: '100%', height: '100%', overflow: 'hidden', border: 'none' }}
                src="https://bnds.us/x81k5l"
                title="Events"
            />
        </Collection>
    )
}
